Eslint configuration for our organization based on
airbnb, flowtype:recommended and react-hooks

## Installation
> `yarn add --dev @rchs/eslint-config`

## Usage
Create `.eslintrc` on your project root with the
following content:
```json
{
  "extends": "@rchs"
}
```
